# ---- server ----
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
# Optional: where appointment/contact notifications are emailed.
# Leave blank to log submissions to stdout instead (default dev behaviour).
SMTP_URL=
NOTIFY_EMAIL=frontdesk@drdivyafetalmedicine.com

# ---- client (build-time) ----
VITE_API_URL=http://localhost:4000
VITE_GOOGLE_MAPS_EMBED_URL=
